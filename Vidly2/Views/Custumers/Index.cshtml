@model IEnumerable<Vidly.Models.Customer>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Customers</h2>
<table class="table table-bordered table-hover">
    <thead>
    <tr>
        <th>Customers</th>
        <th>Membership Type</th>
    </tr>
    </thead>
    <tbody>
    @if (!Model.Any())
    {
        <tr>
            <td>
                Não existem clientes cadastrados
            </td>
        </tr>
    }
    else
    {
        foreach (var custumer in Model)
        {
            <tr>
                <td>@Html.ActionLink(custumer.Name, "Edit", "Custumers", new {id = custumer.Id}, null)</td>
                <td>@custumer.MembershipType.Name</td>
            </tr>
        }
    }
    
    </tbody>
</table>